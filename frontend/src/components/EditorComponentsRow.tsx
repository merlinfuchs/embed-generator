import { ComponentActionRow } from "../discord/types";

interface Props {
  index: number;
  row: ComponentActionRow;
}

export default function EditorComponentsRow({ row, index }: Props) {
  return <div></div>;
}
