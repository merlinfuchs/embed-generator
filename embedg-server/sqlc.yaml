version: 2
sql:
  - engine: "postgresql"
    schema:
      - "db/postgres/migrations"
    queries: "db/postgres/queries"
    gen:
      go:
        package: "pgmodel"
        out: "db/postgres/pgmodel"
