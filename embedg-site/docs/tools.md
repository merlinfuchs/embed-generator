---
sidebar_position: 5
description: Free Discord tools from Embed Generator, a colored text generator using ANSI codes, embed links for sharing rich embeds, and a webhook info lookup.
---

# Tools

Besides the message editor, Embed Generator has a few small free tools. None of them need an account.

## Colored Text Generator

Discord renders ANSI color codes inside code blocks. The [Colored Text Generator](https://message.style/app/tools/colored-text) lets you type your text, pick foreground and background colors per word, and copy the result as a code block you can paste into any Discord message.

## Embed Links

[Embed Links](https://message.style/app/tools/embed-links) turn a title, description, image and color into a URL. When that URL is posted in Discord, it unfurls into an embed, so you can share a rich preview without a webhook or bot. Only a subset of embed fields is supported, since Discord builds the preview from the page's meta tags.

## Webhook Info

Paste any Discord webhook URL into [Webhook Info](https://message.style/app/tools/webhook-info) to see the webhook's name, avatar and who created it. Handy when you find an old URL and don't remember what it was for.
