/** The card a top level component sits in. */
export const CARD = "bg-dark-3 px-3 md:px-4 py-3 mb-3 rounded-md shadow";

/** The same card, for components that hold a list of their own. */
export const PADDED = "bg-dark-3 p-3 rounded-md";

/** A nested child inside a container or section. */
export const NESTED_CARD = `${CARD} border-2 border-dark-5`;
